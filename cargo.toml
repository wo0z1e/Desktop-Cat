[package]
name = "desktop-cat"
version = "0.1.0"
edition = "2021"

# Your project dependencies (like eframe, etc.) will go here

[dependencies]
eframe = "0.26"
egui_extras = { version = "0.26", features = ["image"] }
image = { version = "0.25", features = ["png", "jpeg"] } # or whatever version matches your project